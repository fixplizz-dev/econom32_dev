# Модернизация сайта Департамента экономического развития Брянской области

Проект полной модернизации официального сайта [econom32.ru](https://econom32.ru) с переходом с устаревшей платформы Parser 3 на современный стек технологий.

## 🎯 Цели проекта

- Переход с Parser 3 на Next.js + PostgreSQL
- Современный адаптивный дизайн (тренды 2025)
- Соответствие требованиям доступности WCAG 2.1 AA
- Многоязычность (русский/английский)
- Соответствие ФЗ-8, ФЗ-9 и требованиям безопасности
- Сохранение всех данных при миграции

## 🏗️ Архитектура

### Текущая система (Parser 3)
- **Технология:** Parser 3 + MySQL
- **Админка:** `/manage/`
- **Данные:** Новости с 2014 года, богатая медиабиблиотека
- **Статистика:** Webalizer с детальными логами

### Новая система
- **Frontend:** Next.js 14 + TypeScript + Tailwind CSS
- **Backend:** Express.js + PostgreSQL + Prisma ORM
- **Файлы:** MinIO (S3-compatible)
- **Кэш:** Redis
- **Мониторинг:** Prometheus + Grafana
- **Безопасность:** ClamAV, WAF, SSL/TLS

## 🚀 Быстрый старт

### Предварительные требования

- Docker Desktop
- Node.js 20+
- Git

### Настройка локального окружения

1. **Клонирование и настройка:**
   ```bash
   git clone https://github.com/fixplizz-dev/econom32_dev.git
   cd econom32_dev
   make setup
   ```

2. **Настройка переменных окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл с вашими настройками
   ```

3. **Запуск сервисов:**
   ```bash
   make start
   ```

### Доступные сервисы

После запуска `make start` будут доступны:

| Сервис | URL | Описание |
|--------|-----|----------|
| PostgreSQL | `localhost:5432` | Основная база данных |
| Redis | `localhost:6379` | Кэширование |
| MinIO | http://localhost:9001 | Файловое хранилище |
| Adminer | http://localhost:8080 | Управление БД |
| Prometheus | http://localhost:9090 | Мониторинг |
| Grafana | http://localhost:3000 | Дашборды |
| Nginx | http://localhost:80 | Reverse proxy |

### Учетные данные по умолчанию

- **MinIO:** `econom32_minio` / `econom32_minio_password`
- **Grafana:** `admin` / `admin123`
- **PostgreSQL:** `econom32_user` / `econom32_password`

## 📋 Команды разработки

```bash
# Управление окружением
make setup      # Первоначальная настройка
make start      # Запуск всех сервисов
make stop       # Остановка сервисов
make restart    # Перезапуск
make status     # Статус сервисов
make logs       # Просмотр логов
make clean      # Удаление контейнеров
make reset      # Полная очистка

# Работа с базами данных
make db-shell   # Подключение к PostgreSQL
make redis-cli  # Подключение к Redis

# Просмотр логов конкретного сервиса
make logs-postgres
make logs-nginx
make logs-minio
```

## 🗂️ Структура проекта

```
econom32_dev/
├── docker/                        # Docker конфигурации
│   ├── nginx/                     # Nginx настройки
│   ├── postgres/                  # PostgreSQL инициализация
│   ├── prometheus/                # Мониторинг
│   └── grafana/                   # Дашборды
├── scripts/                       # Скрипты управления
├── frontend/                      # Next.js приложение (будет создано)
├── backend/                       # Express.js API (будет создано)
├── docker-compose.yml             # Основная конфигурация
├── .env.example                   # Пример переменных окружения
├── Makefile                       # Команды разработки
└── GITHUB_SETUP.md               # Инструкции по настройке GitHub
```

## 🏗️ Этапы разработки

### Фаза 1: Разработка нового сайта (текущая)

1. ✅ **Настройка инфраструктуры** - Docker, PostgreSQL, Redis, MinIO
2. 🔄 **Создание архитектуры** - Next.js frontend, Express.js backend
3. 📋 **Разработка функциональности** - 29 задач для реализации
4. 📋 **Тестирование** - unit, integration, E2E тесты
5. 📋 **Оптимизация** - производительность, безопасность, SEO

### Фаза 2: Миграция данных (после завершения разработки)

**Будет выполнена только после полной готовности нового сайта:**

1. **Анализ данных для миграции:**
   - Таблица `news` → новости с 2014 года
   - Таблица `menus` → структура навигации
   - Таблица `bcatalog` → каталоги
   - Таблица `global_setting` → настройки сайта
   - Папка `/bank/` → медиафайлы и документы

2. **Этапы миграции:**
   - Экспорт данных из Parser 3 + MySQL
   - Трансформация в новый формат PostgreSQL
   - Импорт в новую систему
   - Настройка редиректов для сохранения SEO
   - Тестирование корректности миграции

## 🔧 Разработка

### Следующие шаги для разработки

1. **Настройка проекта Next.js:**
   ```bash
   npx create-next-app@latest frontend --typescript --tailwind --app
   cd frontend
   npm install @prisma/client next-auth
   ```

2. **Настройка backend API:**
   ```bash
   mkdir backend && cd backend
   npm init -y
   npm install express prisma @prisma/client bcrypt jsonwebtoken
   npm install -D @types/node typescript ts-node
   ```

3. **Инициализация Prisma:**
   ```bash
   npx prisma init
   # Настройте schema.prisma согласно спецификациям
   npx prisma generate
   npx prisma db push
   ```

### План реализации (29 задач)

1. ✅ Настройка инфраструктуры (Docker, PostgreSQL, Redis)
2. 🔄 Система аутентификации и безопасности
3. 🔄 Модели данных и миграции
4. 🔄 API для управления контентом
5. 🔄 Система интернет-приемной
6. ... и другие задачи

## 🛡️ Безопасность

- HTTPS с автообновляемыми SSL сертификатами
- WAF защита от атак
- Антивирусная проверка файлов (ClamAV)
- Rate limiting и защита от брутфорса
- Валидация и санитизация данных
- Регулярные бэкапы с шифрованием

## 📈 Мониторинг

- **Prometheus** - сбор метрик
- **Grafana** - визуализация и дашборды
- **Health checks** - проверка состояния сервисов
- **Логирование** - централизованные логи
- **Алерты** - уведомления о проблемах

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для задачи: `git checkout -b feature/task-name`
3. Реализуйте функциональность
4. Создайте Pull Request

## 📞 Поддержка

При возникновении вопросов:

1. Проверьте документацию в репозитории
2. Посмотрите логи: `make logs`
3. Проверьте статус сервисов: `make status`
4. Создайте Issue в репозитории

## 🚀 Версионирование

- **v0.1** - ✅ Инициализация проекта и инфраструктуры
- **v0.2** - 🔄 Создание Next.js приложения (следующий этап)
- **v0.3** - 📋 Настройка Prisma и PostgreSQL схемы
- **v0.4** - 📋 Система аутентификации
- **v0.5** - 📋 API для управления контентом
- ... продолжение по задачам

---

**Статус проекта:** 🚧 В разработке (этап создания нового сайта)  
**Текущая версия:** v0.1  
**GitHub:** https://github.com/fixplizz-dev/econom32_dev  
**Лицензия:** Государственный проект